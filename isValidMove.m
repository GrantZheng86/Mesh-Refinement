function toReturn = isValidMove(F,V,Vi,Vnew)
    l = length(F);
    for i = 1:l
        if (F(i,1) == Vi || F(i,2) == Vi || F(i,3) == Vi)
            % Check Jacobian
            
        
    end
end

